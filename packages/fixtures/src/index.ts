import * as events from './events';
import fields from './fields';

// Plans to add more fixtures like `web` for responses
// eslint-disable-next-line import/prefer-default-export
export { events, fields };
